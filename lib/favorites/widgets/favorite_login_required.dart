import 'package:flutter/material.dart';

class FavoriteLoginWidget extends StatelessWidget {
  const FavoriteLoginWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Please login to see favorites"),
    );
  }
}